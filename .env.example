APIFY_TOKEN=replace_with_your_apify_token
# Optional. Without it the inventory reader uses the public CSV export,
# which needs the sheet shared as "anyone with the link can view".
GOOGLE_SHEETS_API_KEY=replace_with_your_google_sheets_api_key
